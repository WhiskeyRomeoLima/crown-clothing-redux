//the place to store actions types associated with user (could be setting the user, updating the user, deleting the user, etc.)
const USER_ACTION_TYPES = {
  SET_CURRENT_USER: 'user/SET_CURRENT_USER',
};

export default USER_ACTION_TYPES;
